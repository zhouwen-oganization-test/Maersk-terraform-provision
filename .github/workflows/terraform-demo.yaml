---
  name: demo workflow 
  on:
    workflow_dispatch:
    pull_request:
      paths:
        - demo/demo-account/demo-application/**
        - .github/workflows/terraform-demo.yml
    push:
      paths:
        - demo/demo-account/demo-application/**
        - .github/workflows/terraform-demo.yml
     
  permissions:
    id-token: write # This is required for requesting the JWT
    contents: read  # This is required for actions/checkout
    
  jobs:
    path-filter:
      name: path-filter 
      runs-on: ubuntu-latest
      outputs: 
        network: ${{ steps.changes.outputs.network }}
        resources: ${{ steps.changes.outputs.resources }}
      steps:
        - name: Check out code
          uses: actions/checkout@v3
        - name: Change filter
          uses: dorny/paths-filter@v2
          id: changes
          with:
             filters: |
                network:
                  - 'demo/demo-account/demo-application/network/**'
                resources:
                  - 'demo/demo-account/demo-application/resources/**'
    terraform-network:
      needs: path-filter
      if: ${{ needs.path-filter.outputs.network }} == 'true'
      uses: ./.github/workflows/reusable-terraform-workflow.yaml
      with:
        path: demo/demo-account/demo-application/network
        environment: non-prod
      secrets: inherit
    terraform-resources:
      needs: 
        - path-filter
        - terraform-network
      if: ${{ needs.path-filter.outputs.resources }} == 'true'
      uses: ./.github/workflows/reusable-terraform-workflow.yaml
      with:
        path: demo/demo-account/demo-application/resources
        environment: non-prod
      secrets: inherit